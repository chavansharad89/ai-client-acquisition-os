{
  "name": "@acos/tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config fixtures/vitest.config.ts && vitest run --config pipeline/vitest.config.ts",
    "test:integration": "vitest run --config integration/vitest.config.ts",
    "test:contract": "vitest run --config contract/vitest.config.ts"
  },
  "dependencies": {
    "@acos/catalog": "workspace:*",
    "@acos/core-capi": "workspace:*",
    "@acos/core-entitlements": "workspace:*",
    "@acos/core-payments": "workspace:*",
    "@acos/core-reconciliation": "workspace:*",
    "@acos/db": "workspace:*",
    "@acos/db-index-deploy": "workspace:*",
    "@acos/rate-limit": "workspace:*",
    "@acos/worker": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
