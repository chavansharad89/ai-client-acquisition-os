{
  "name": "@acos/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no runtime logic to test in @acos/shared-types' && exit 0",
    "test:integration": "echo 'no integration tests owned directly by @acos/shared-types' && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
