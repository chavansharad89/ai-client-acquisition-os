{
  "name": "@acos/core-entitlements",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "echo 'no integration tests owned directly by @acos/core-entitlements' && exit 0"
  },
  "dependencies": {
    "@acos/catalog": "workspace:*",
    "@acos/db": "workspace:*",
    "@acos/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
