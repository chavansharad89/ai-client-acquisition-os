{
  "name": "@acos/core-research",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "echo 'integration tests live in /tests/integration' && exit 0"
  },
  "dependencies": {
    "@acos/core-acquisition": "workspace:*",
    "@anthropic-ai/sdk": "^0.125.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
