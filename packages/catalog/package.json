{
  "name": "@acos/catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Strongly typed, server-authoritative product catalog for the funnel (Starter Kit -> Launch Kit -> Client Acquisition System). The client never determines price \u2014 it only ever sends a product id, and the server resolves that id against this catalog.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "echo 'no integration tests owned directly by @acos/catalog' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
